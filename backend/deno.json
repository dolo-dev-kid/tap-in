{
  "tasks": {
    "dev": "deno run -A --watch server.ts",
    "build": "deno cache server.ts",
    "start": "deno run -A server.ts",
    "test": "deno test -A",
    "lint": "deno lint",
    "fmt": "deno fmt"
  }
}
